# Launch tweets

## Tweet 1

I am building `run-all-now`: ultra-fast native replacement for npm-run-all.

The goal is not a rewrite. The goal is startup time, lower memory, and benchmarks you can reproduce.

## Tweet 2

Benchmark rule for `run-all-now`:

No screenshots without scripts. No speed claims without hardware, commit SHA, and raw commands.

## Tweet 3

Legacy Node devtools are great until every command pays the Node startup tax.

`run-all-now` moves the hot path into Rust and keeps npm install DX.

## Tweet 4

Side-by-side GIF coming next: `npm-run-all` on the left, `run-all-now` on the right.

Wall time, CPU, and memory visible. No vibes-only performance claims.
