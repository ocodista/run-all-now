# @run-all-now/linux-x64

Native Rust binary package for `run-all-now` on `linux-x64`.

This package is installed automatically as an optional dependency of `run-all-now`. You usually should not install it directly.
